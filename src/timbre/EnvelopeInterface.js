import $ from 'jquery'
import PIXI, { Container, Graphics } from 'pixi.js'

const adsrInstances = [];

export function init() {
	$('[data-adsr]').each((i, elem) => {
		const canvas = document.createElement('canvas');

	});
}